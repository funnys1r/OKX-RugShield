# Modes

## Safe Mode

Default mode.

- Require confirmation before any execution-like action.
- Prefer staged exits.
- Show assumptions and route risks.

## Auto-Defense Mode

Treat as conditional capability, not a promise.

- Only describe as available if the runtime truly supports it.
- Still report why the action would trigger.
- If execution cannot be verified, downgrade to simulation or recommendation mode.
